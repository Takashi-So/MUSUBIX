# Budget Tracker System - 要件定義書 v2.0

**Project ID**: project-13-budget-tracker
**Document ID**: REQ-001
**Version**: 2.0 (Revised after Review)
**Created**: 2026-01-04
**Status**: ✅ Approved

---

## 1. ビジネス概要

### 1.1 プロジェクト目的
個人およびチーム向けの予算管理システムを構築し、収支の可視化と予算遵守を支援する。

### 1.2 ビジネス目標
- 予算計画の立案と追跡を容易にする
- 支出パターンの分析により節約機会を特定する
- 予算超過を事前に警告し、財務健全性を維持する

### 1.3 ステークホルダー
| ステークホルダー | 関心事 |
|-----------------|--------|
| エンドユーザー | 簡単な予算管理、直感的なUI |
| システム管理者 | 安定稼働、データ保全 |
| 開発チーム | 保守性、拡張性 |

---

## 2. 基本定義（レビュー指摘対応: ISSUE-003, ISSUE-004）

### 2.1 金額（Money）の定義
| 項目 | 定義 |
|------|------|
| 通貨 | JPY（日本円）固定（v1.0） |
| 最小単位 | 1円（小数なし） |
| 最大値 | 999,999,999円 |
| 丸め方法 | N/A（整数のみ） |

### 2.2 予算期間（Budget Period）の定義
| 項目 | 定義 |
|------|------|
| 期間タイプ | 暦月（1日〜末日） |
| タイムゾーン | Asia/Tokyo (JST) |
| 月の開始 | 毎月1日 00:00:00 JST |
| 月の終了 | 月末日 23:59:59 JST |

### 2.3 ID形式の統一（レビュー指摘対応: ISSUE-005）
| エンティティ | 形式 | 例 |
|-------------|------|-----|
| User | USR-XXXXXXXX | USR-A1B2C3D4 |
| Category | CAT-YYYYMMDD-NNN | CAT-20260104-001 |
| Expense | EXP-YYYYMMDD-NNN | EXP-20260104-001 |
| Alert | ALT-YYYYMMDD-NNN | ALT-20260104-001 |

---

## 3. 機能要件（EARS形式）

### 3.1 ユーザー認証（レビュー指摘対応: ISSUE-002）

#### REQ-BT-040: ユーザー登録
**Priority**: P0 (必須)
**EARS Pattern**: Event-driven

```
WHEN a person requests to register,
THE system SHALL create a user account with email and password,
AND assign a unique user ID in format USR-XXXXXXXX.
```

**Acceptance Criteria**:
- メールアドレスは有効な形式
- パスワードは8文字以上、英数字混合
- メールアドレスの重複不可

#### REQ-BT-041: ユーザーログイン
**Priority**: P0 (必須)
**EARS Pattern**: Event-driven

```
WHEN a user provides valid credentials,
THE system SHALL authenticate the user,
AND return an access token valid for 24 hours.
```

#### REQ-BT-042: ユーザーログアウト
**Priority**: P1 (重要)
**EARS Pattern**: Event-driven

```
WHEN a user requests to logout,
THE system SHALL invalidate the user's current access token.
```

---

### 3.2 予算カテゴリ管理

#### REQ-BT-001: カテゴリ作成
**Priority**: P0 (必須)
**EARS Pattern**: Event-driven

```
WHEN an authenticated user requests to create a budget category,
THE system SHALL create a new category with name, description, and monthly limit,
AND assign a unique category ID in format CAT-YYYYMMDD-NNN.
```

**Acceptance Criteria**:
- カテゴリ名は1-50文字
- 月間上限金額は1円以上999,999,999円以下
- 同一ユーザー内でカテゴリ名の重複不可

#### REQ-BT-002: カテゴリ一覧取得
**Priority**: P0 (必須)
**EARS Pattern**: Event-driven

```
WHEN an authenticated user requests their budget categories,
THE system SHALL return all active categories owned by the user,
AND include current month spending amount for each category.
```

#### REQ-BT-003: カテゴリ更新
**Priority**: P1 (重要)
**EARS Pattern**: Event-driven

```
WHEN an authenticated user requests to update a budget category,
THE system SHALL update the category's name, description, or monthly limit,
AND log the change in the category's audit history.
```

**Note**: 変更履歴（audit history）には変更日時、変更前後の値を記録

#### REQ-BT-004: カテゴリアーカイブ
**Priority**: P1 (重要)
**EARS Pattern**: Event-driven

```
WHEN an authenticated user requests to archive a budget category,
THE system SHALL set the category status to 'archived',
AND NOT allow new expenses to be recorded to the archived category.
```

**Deletion Policy**: 全エンティティは論理削除（アーカイブ）を採用（レビュー指摘対応: ISSUE-006）

---

### 3.3 支出記録管理

#### REQ-BT-010: 支出記録作成
**Priority**: P0 (必須)
**EARS Pattern**: Event-driven

```
WHEN an authenticated user records an expense,
THE system SHALL create an expense record with amount, category, date, and description,
AND assign a unique expense ID in format EXP-YYYYMMDD-NNN.
```

**Acceptance Criteria**:
- 金額は1円以上999,999,999円以下
- 日付は過去365日以内または当日のみ許可
- カテゴリは既存の有効（active）なカテゴリのみ

#### REQ-BT-011: 支出記録一覧取得
**Priority**: P0 (必須)
**EARS Pattern**: Event-driven

```
WHEN an authenticated user requests their expense records,
THE system SHALL return expenses filtered by date range and/or category,
AND sort by date in descending order by default.
```

#### REQ-BT-012: 支出記録更新
**Priority**: P1 (重要)
**EARS Pattern**: Event-driven

```
WHEN an authenticated user requests to update an expense record,
THE system SHALL update the expense's amount, category, date, or description,
AND recalculate the affected category's spending totals.
```

#### REQ-BT-013: 支出記録アーカイブ
**Priority**: P1 (重要)
**EARS Pattern**: Event-driven

```
WHEN an authenticated user requests to archive an expense record,
THE system SHALL set the expense status to 'archived',
AND recalculate the affected category's spending totals excluding archived expenses.
```

---

### 3.4 予算分析

#### REQ-BT-020: 月次サマリー取得
**Priority**: P0 (必須)
**EARS Pattern**: Event-driven

```
WHEN an authenticated user requests a monthly budget summary,
THE system SHALL calculate and return:
  - Total budget (sum of all active category limits)
  - Total spending (sum of all active expenses in the period)
  - Remaining budget (total budget - total spending)
  - Spending percentage per category.
```

#### REQ-BT-021: カテゴリ別分析
**Priority**: P1 (重要)
**EARS Pattern**: Event-driven

```
WHEN an authenticated user requests category analysis for a specific period,
THE system SHALL return:
  - Spending trend (daily totals over the period)
  - Average daily spending
  - Comparison with previous equivalent period.
```

#### REQ-BT-022: 予算警告状態（レビュー指摘対応: ISSUE-001 - 分割1）
**Priority**: P0 (必須)
**EARS Pattern**: State-driven

```
WHILE a category's spending is between 80% and 99% of its monthly limit,
THE system SHALL set the category's budget status to 'warning'.
```

#### REQ-BT-023: 予算超過状態（レビュー指摘対応: ISSUE-001 - 分割2）
**Priority**: P0 (必須)
**EARS Pattern**: State-driven

```
WHILE a category's spending is 100% or more of its monthly limit,
THE system SHALL set the category's budget status to 'exceeded'.
```

---

### 3.5 アラート通知（レビュー指摘対応: ISSUE-008）

#### REQ-BT-030: 予算警告アラート生成
**Priority**: P1 (重要)
**EARS Pattern**: Event-driven

```
WHEN a category's spending first reaches 80% of its monthly limit within a budget period,
THE system SHALL create a warning alert record,
AND deliver the alert via in-app notification.
```

#### REQ-BT-031: 予算超過アラート生成
**Priority**: P1 (重要)
**EARS Pattern**: Event-driven

```
WHEN a category's spending first exceeds 100% of its monthly limit within a budget period,
THE system SHALL create an exceeded alert record,
AND deliver the alert via in-app notification.
```

**Notification Channels (v1.0)**:
- In-app notification: ✅ 実装
- Email notification: 🔮 将来バージョン
- Push notification: 🔮 将来バージョン

#### REQ-BT-032: アラート確認
**Priority**: P2 (任意)
**EARS Pattern**: Event-driven

```
WHEN an authenticated user acknowledges an alert,
THE system SHALL set the alert status to 'read',
AND record the acknowledgment timestamp.
```

#### REQ-BT-033: 重複アラート防止
**Priority**: P1 (重要)
**EARS Pattern**: Unwanted

```
THE system SHALL NOT generate duplicate alerts for the same category 
and threshold combination within the same budget period.
```

---

## 4. 非機能要件

### 4.1 パフォーマンス

#### REQ-BT-NFR-001: レスポンスタイム
**Priority**: P1 (重要)
**EARS Pattern**: Ubiquitous

```
THE system SHALL respond to API requests within 200ms for 95% of requests.
```

#### REQ-BT-NFR-002: データ容量
**Priority**: P1 (重要)
**EARS Pattern**: Ubiquitous

```
THE system SHALL support up to 10,000 expense records per user.
```

### 4.2 セキュリティ

#### REQ-BT-NFR-010: データ分離
**Priority**: P0 (必須)
**EARS Pattern**: Ubiquitous

```
THE system SHALL ensure authenticated users can only access their own budget data.
```

#### REQ-BT-NFR-011: 入力検証
**Priority**: P0 (必須)
**EARS Pattern**: Ubiquitous

```
THE system SHALL validate all user inputs to prevent SQL injection and XSS attacks.
```

### 4.3 信頼性

#### REQ-BT-NFR-020: データ整合性
**Priority**: P0 (必須)
**EARS Pattern**: Ubiquitous

```
THE system SHALL maintain consistency between expense records and category spending totals 
using transactional operations.
```

---

## 5. ユースケース

### UC-001: 月間予算を設定する
**Actor**: 認証済みユーザー
**Precondition**: ユーザーがログイン済み
**Main Flow**:
1. ユーザーがカテゴリ作成を選択
2. カテゴリ名と月間上限を入力
3. システムがカテゴリを作成
4. 確認メッセージを表示

### UC-002: 支出を記録する
**Actor**: 認証済みユーザー
**Precondition**: 1つ以上の有効なカテゴリが存在
**Main Flow**:
1. ユーザーが支出記録を選択
2. 金額、カテゴリ、日付、説明を入力
3. システムが支出を記録
4. カテゴリの残予算を表示
5. 予算状態を更新（必要に応じてアラート生成）

### UC-003: 予算状況を確認する
**Actor**: 認証済みユーザー
**Precondition**: 1つ以上のカテゴリと支出が存在
**Main Flow**:
1. ユーザーがダッシュボードを表示
2. システムが月次サマリーを表示
3. カテゴリ別の予算消化率を表示
4. 未読アラートがあれば表示

---

## 6. 用語集

| 用語 | 定義 |
|------|------|
| Budget Category | 予算を管理する単位（食費、交通費等） |
| Monthly Limit | カテゴリごとの月間予算上限（円） |
| Budget Period | 予算の計算期間（暦月） |
| Expense | 個別の支出記録 |
| Spending | 特定期間内の支出合計 |
| Budget Status | カテゴリの予算状態（normal/warning/exceeded） |
| Alert | 予算状況に関するアプリ内通知 |
| Audit History | エンティティの変更履歴 |

---

## 7. トレーサビリティマトリクス

| 要件ID | 優先度 | 設計ID | テストID |
|--------|--------|--------|----------|
| REQ-BT-040 | P0 | DES-AUTH-001 | TST-AUTH-001 |
| REQ-BT-041 | P0 | DES-AUTH-002 | TST-AUTH-002 |
| REQ-BT-001 | P0 | DES-CAT-001 | TST-CAT-001 |
| REQ-BT-002 | P0 | DES-CAT-002 | TST-CAT-002 |
| REQ-BT-010 | P0 | DES-EXP-001 | TST-EXP-001 |
| REQ-BT-020 | P0 | DES-ANA-001 | TST-ANA-001 |
| REQ-BT-022 | P0 | DES-STA-001 | TST-STA-001 |
| REQ-BT-023 | P0 | DES-STA-002 | TST-STA-002 |
| REQ-BT-030 | P1 | DES-ALT-001 | TST-ALT-001 |

---

## 8. レビュー対応履歴

| Issue ID | 問題 | 対応 |
|----------|------|------|
| ISSUE-001 | REQ-BT-022複合条件 | REQ-BT-022とREQ-BT-023に分割 |
| ISSUE-002 | 認証要件欠落 | REQ-BT-040〜042を追加 |
| ISSUE-003 | 金額定義不明確 | セクション2.1で定義 |
| ISSUE-004 | 月の定義曖昧 | セクション2.2で定義 |
| ISSUE-005 | ID形式不統一 | セクション2.3で統一定義 |
| ISSUE-006 | 削除ポリシー不統一 | 全エンティティ論理削除に統一 |
| ISSUE-007 | spending history不明確 | audit historyとして明確化 |
| ISSUE-008 | 通知方法未定義 | in-app通知として明確化 |

---

**Document History**:
| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0 | 2026-01-04 | AI Architect | Initial draft |
| 2.0 | 2026-01-04 | AI Architect | Review feedback incorporated |

**Approval**:
- [x] Requirements complete
- [x] EARS format compliant
- [x] Review issues resolved
- [x] Ready for Design Phase
